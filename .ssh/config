# Personal
Host lanijw
    HostName github.com
    User git                    # Default central user for Git(flavor) instance
    IdentityFile ~/.ssh/lanijw
    IdentitiesOnly yes          # Try other SSH keys as well.
